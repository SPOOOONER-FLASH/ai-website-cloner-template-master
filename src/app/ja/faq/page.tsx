import type { Metadata } from "next";
import { FaqPage, faqMetadata } from "@/components/locale-pages/FaqPage";

/** GENERATED by scripts/scaffold-locale-routes.mjs — edit the template there, not this file. */
export const metadata: Metadata = faqMetadata("ja");

export default function Page() {
  return <FaqPage locale="ja" />;
}
