# 给工厂的问题清单 — 2026-09-08（09-09 按需求数据重排优先级）

> **2026-09-09 更新：优先级变了，因为有了需求数据。**
> 下面原本按「缺什么」排列，现在按「缺它挡住多少已经存在的询盘」重排。
> 依据见 `docs/research/2026-09-09-demand-analysis-and-fair-shortlist.md`。
>
> | 优先级 | 缺什么 | 挡住的真实需求 |
> |---|---|---|
> | **最高** | **6068 与窄边锁体的规格表** | 各 **3 个询盘**，阿里排行并列第二，而网站上**没有页面** |
> | 高 | 主匙系统能做几组几级 | 阿里曝光 **259**，全表最高，站上零字 |
> | 高 | 307/305/310/301/316-S 的尺寸 | 五张尺寸线图；BAU 规格制定者要的就是图 |
> | 中 | 308 的规格与照片 | 阿里 37 次曝光，该页现为 noindex |
> | 中 | MB / CP / BS 之外还有没有别的自定义表面码 | 已答复三个，见下 |
>
> **前三条卡的是同一件事：规格数据。** 不是设计、不是推广、不是外链。

---


每一条都是**目录里已有的东西，但我们不能替它回答**。凡是能从现有数据推导的都已经
自动做掉了；剩在这里的，推导会变成编造。

---

## ✅ 已解决（一半）：LC 锁体配 072 逃生锁（甲方 2026-09-09 答复）

甲方原话：

> LC 锁体配 072 逃生锁，072 防火锁体有两个葫芦孔的。

这回答了 `2026-09-09-top20-image-brief.md` 第一梯队第 3 组卡住的那个问题 ——
插芯锁体和逃生推杠之间的配套关系。307 的 features 里本来就写着 `• Lock Case: 072`，
现在多了两条：**LC 系列锁体是配 072 的那一侧**，以及 **072 有一个防火版本，
锁体上是两个葫芦孔**（双锁芯孔位）。

### 但这里冒出一个更要紧的缺口：072 在网站上没有页面

| | |
|---|---|
| 目录里搜 `072` | 只有 `ju-072-door-closer`（闭门器），不是这个 072 |
| 307 的页面上 | 写着 `Lock Case: 072`，点不动，因为没有那一页 |
| 307 是什么 | **阿里询盘第一（5 个），转化 18.18%，自有模具** |

也就是说：站上曝光最高的那个产品，它的规格行点名的配套锁体，买家在站上找不到。
这不是作图能补的，也不是文案能补的 —— **072 需要一条产品记录**（照片 + 规格）。
按需求排，它现在的优先级高于本文件里其余任何一条。

### 还需要工厂一句话才能写进产品页

1. **是「LC 系列都配 072」还是特定几个 LC 型号？** 目录里有 **42 个 LC 型号**
   （LC02 到 LC9230，中心距 45/50/55/60/65/70/72mm 各有）。把这句话原样写到 42 个
   页面上，等于替工厂对 42 组配套关系背书 —— 错一组就是买家订到装不上的锁体。
   给一份「配 072 的 LC 型号清单」就能一次全部上线。
2. **防火版 072 的型号叫什么？** 是 072 本身有防火版，还是另一个编号（072F / 072-FR）？
   两个葫芦孔的中心距是多少？
3. **072 的照片和尺寸**（面板长宽、backset、葫芦孔中心距）。

---

## ✅ 已解决：DS011 是门吸（工厂 2026-09-08 答复）

已改名 `Door Flush Bolt` → `Door Stopper`，slug `ds011-door-flush-bolt` →
`ds011-door-stopper`，11 个图片视频文件跟着改名，旧 URL 留了 301。
用 `scripts/rename-product-slug.mjs` 做的，不是手改。

---

## 一、尺寸线图：能画的已经画了，剩下的缺数字

甲方 2026-09-08 提到「可以自己画点 CAD 上去」。**画不了，缺的不是工具是数字。**

`scripts/build-dimension-drawings.mjs` 已经在跑，出了 79 张 1:1 的尺寸线图 ——
但它只对**目录里已发布尺寸**的产品出图，没有数字就返回 null 不画。

一张线图上的每条尺寸线都会被买家当作可下单的依据。凭外观估出来的中心距、
backset 或孔位，画得越好看越危险：买家照着订，metal 到手装不上，一个柜的损失。
这跟拒绝替 MB / CP / BS 猜表面名是同一条规矩。

**需要什么才能画**：每个型号给到中心距、backset、面板长宽、孔位。给了就跑
`npm run assets:drawings`，当天出图。

优先级最高的是这 12 个刚补上照片但规格全空的：
`5687`、`5834`、`L002`、`L008`、`L009`、`L010`、`L016`、`L019`、`L023`、
`L024`、`L025`、`L026`。

---

## 二、（原一）DS011 到底是门吸还是插销？— 已答复，见上

| | |
|---|---|
| 目录里的名字 | `Door Flush Bolt`（插销） |
| URL | `/products/hardware-accessories/ds011-door-flush-bolt/` |
| 同前缀的另外 7 条 | 全部叫 `Door Stopper`（门吸） |
| 规格 | 只有「Material=Brass」加一串表面代码，**没有尺寸** |

**反向证据**：Bing 2026-09-08 关键词报告里有一条真实买家搜索 ——
`door stopper ds013 vs ds011`，把 DS011 当门吸和 DS013 比较。

没有改。前缀和搜索词都不足以判定一个产品是什么，改错了就是买家订到装不上的东西。
`src/data/product-naming.test.ts` 里有一条显式待办把它记着，工厂答复后删掉那一行、
改名字并加 301。

---

## 二、BS 和 SB 是同一个表面的两个代码

- `SB: "Satin Brass"` —— 目录既有的商品名表
- `BS` —— 甲方 2026-09-08 确认「satin brass, 铜砂光」，九月的产品文件夹也用这个拼法

两个都已收进 `src/lib/configurator.ts`，因为挑一个当正确的会让另一个在配置器里显示成
裸代码。**要不要让工厂退掉其中一个，是工厂的决定。**

---

## 三、还缺规格的产品

`npm run sheets` 出两张填写表。当前状态：

| | 数量 |
|---|---|
| 目录记录 | 573 |
| 有照片（已发布） | 475 |
| 无照片（已建页但 noindex） | 98 |

98 条里有 64 条是这两天用照片建的新记录，规格表是空的——页面上写着「完整规格正在与
工厂确认」。每补一条规格就多一页能被索引的内容。

---

## 四、空文件夹

`5687`、`5834`、以及 Latches 里的十个 `L0xx` —— 微信 zip 里这些文件夹是空的，没有照片
也没有视频。是漏传了还是这些型号已停产？
