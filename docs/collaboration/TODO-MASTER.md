# cantonlock.com 待办总表

**最后更新：2026-09-22 · 更新人：Claude**

这份文件存在的唯一目的：**把「我提过要改、但可能忘掉」的事全部集中在一处，一项一项做掉。**

规则和 `CLIENT-RUNBOOK.md` 一样 —— 做完的条目移到最后的「已完成」区，不留在待办里。
**你在待办区读到一条已经做完的事，是我的错，告诉我。**

Word 版由 `npm run todo:docx` 生成到 `Desktop\hyde\TODO-MASTER.docx`。
**改内容改这份 markdown，不要改 Word**，下次生成会覆盖。

---

## 一、现在轮到谁

| 谁 | 当前手上的事 |
|---|---|
| **你（Spooner）** | 第 5 项（GA4 过滤器）、第 6 项（贴服务器脚本）、每次发布后 purge |
| **Claude** | 第 3、7、8、9、10、11 项 |
| **Codex** | 额度用尽暂停中。恢复后继续设计与编辑图像 |

---

## 二、待办（按该修的顺序）

### 1. ~~GA4 配关键事件~~ ✅ 已完成 2026-09-22

网站代码已发 `generate_lead`（询盘发送成功时触发），你已在 GA4 把
`generate_lead` 和 `form_start` 标成关键事件。发布构建已上线，**下一个询盘进来
最长 24 小时后会出现第一个数字**。

在此之前所有渠道价值判断都是代理指标 —— 这条做完，后面每个决策才有依据。

### 2. ~~合并 www 与裸域~~ ✅ 已完成 2026-09-22

实测确认：

```
301  https://www.cantonlock.com/products/  →  https://cantonlock.com/products/
301  https://www.cantonlock.com/           →  https://cantonlock.com/
```

原本首页被 Google 算成两个页面（www 31 点击/206 展示 + 裸域 19/101），
现在权重合并。这条作用在**已有的 1,520 次展示**上。

### 3. 产品标题加尺寸 —— 方案已测完，等执行 · **Claude**

**为什么**：展示涨了 6.1 倍而点击只涨 2.3 倍，差距全在搜索结果页那两行字上。
型号查询（fb005 / bh28 / 564mb / d102…）排 3–8 名**零点击**，
`patch fitting` 排第 **1** 名也零点击。

**实测结论**：924 个产品里 **472 个**可以在 60 字符内加上一个**带标签的**尺寸。

关键设计：尺寸**必须带标签**。`1073D Night Latch And Rim Lock, 60mm` 有歧义
（60mm 是 backset 还是锁体尺寸？），改成 `60mm backset`。一个有歧义的数字比
没有数字更糟。

超长时舍材质保尺寸 —— 材质在页面规格表里一定看得到，而 924 个产品里只有
497 个发布了任何可用尺寸。

完整方案：`docs/collaboration/tasks/2026-09-22-product-title-dimensions.md`

**验证方式**：两周后重跑 `npm run seo:digest`，看型号查询点击率有没有动。
**没动就说明我判断错了**，那就不是标题问题，要回头看 `seoDescription`。

### 4. ~~第二批 20 篇 guides 部署~~ ✅ 已完成 2026-09-22

`/guides/` 现在 40 篇全部上线（提交 `1b55999ba77`，16,819 个文件）。
此前第二批源码已提交但从未构建，线上 404。

**此前所有 AI 引用成绩都是 35 篇旧 `/news/` 挣的，新的 40 篇还没开始发力。**
两周后重跑 `seo:digest` 对比 SoA 从 32.11% 走到哪。

### 5. GA4 流量被机器污染 —— 加过滤器 · **你**

**证据**：新加坡 1,396 个「用户」平均互动 **0.15 秒**；中国 1,884 个里也有大量
机器。GA4 报的「3,657 用户」这个数字**没有意义，不能拿去汇报**。

真实受众是**美国 139 个用户、平均互动 182 秒**。

**怎么做**：GA4 → 管理 → 数据流 → 选 hyde → 更多代码设置 → 定义内部流量，
或者在「数据过滤器」里排除已知的机器来源。具体点击路径我会写进
`CLIENT-RUNBOOK.md`，等你说要做的时候。

### 6. 61 个视频不被 Google 收录 —— 等你贴服务器脚本 · **你**

我已排除七项：robots.txt、文件存在、页面有播放器、结构化数据完整、缩略图存在、
文件大小、分段下载支持。**剩下唯一异常是实测到的**：

同一个视频内容一字节没变（线上大小与仓库一致，git 里最后改动是 9/21 凌晨），
但线上「最后修改时间」是 9/22 早上 6:21，四个视频全落在 **06:21:11–06:21:14**
三秒内 —— 整个目录被整批重写过。nginx 用修改时间生成 ETag，所以在 Google 眼里
这些视频每天「变」好几次，收录状态永远定不下来。

**你要做的**（零风险，只是显示文件内容）：

```bash
cat /usr/local/bin/cantonlock-deploy.sh
tail -30 /var/log/cantonlock-deploy.log
```

如果脚本里是 `cp -r` 这种整目录复制，改成 `cp -a` 或 `rsync -a` 即可，一行的事。

### 7. 旧 35 篇文章全部扩写到 ≥1,600 词（三语）· **Claude**

**进度 6 / 35。** 排序依据是 AI 引用数 —— 扩写一篇已经在被引用的文章回报更高。

| 批 | 篇 | 状态 |
|---|---|---|
| 1 | push-bar-or-touch-bar、master-key-systems、handing-left-right | ✅ |
| 2 | finish-codes、mortise-lock-backset、reading-model-numbers | ✅ |
| 3+ | 其余 29 篇 | 待做 |

规模：EN 总缺口约 30,398 词，三语约 91,000 词。
完整顺序表：`docs/collaboration/tasks/2026-09-22-expand-all-35-news-articles.md`

### 8. 西语统一到**拉美西班牙语** · **Claude**

**你 2026-09-22 拍板：倒向拉美。理由是秘鲁、阿根廷是老客户集中地，
西班牙和墨西哥还没接触过。** 这个理由比我原先用的点击率数据更硬 ——
真实客户分布胜过搜索指标。

**现状是混的，而且混在同一个产品页里**：

| 词对 | 词表 | 产品页 | 文章 |
|---|---|---|---|
| manilla / **manija** | 9 / 18 | **179 / 129** | 260 / 42 |
| pomo / **perilla** | 6 / 0 | 69 / 0 | 42 / 0 |
| tirador / **jaladera** | 5 / 0 | 51 / 0 | 18 / 0 |
| cerradero / **contrachapa** | 6 / 0 | 155 / 0 | 101 / 0 |
| retranqueo / **entrada** | 1 / 6 | 0 / 474 | 0 / 119 |

（加粗的是拉美说法，也就是从今往后的标准）

`es-glossary.ts` 文件头**早就声明了拉美**，但只有 `entrada` 一条真的执行了。
其余三对全漂到了欧西，而 `manilla`/`manija` 连产品页内部都不一致。

约 **439 处**待统一。要单独一个提交、带完整 diff 给人读。
详情：`docs/collaboration/tasks/2026-09-22-spanish-variant-is-inconsistent.md`

### 9. 下一批内容写「A vs B」和横评 · **Claude**

**依据**：被引最多的一篇 `push-bar-or-touch-bar` 占全部引用的 **23%**，
因为它回答了一个有两个选项、需要判断、而厂商通常不愿明说的问题。
最高份额的查询是 `box-style exit device vs touch-bar`（46–59%）。

**你的补充**：不止两两对比，可以做三四五个一堆的**横评**，像 YouTube 那种
最热门 SUV 或手机横评。我同意，而且横评更强 —— 没有一个厂商会横评自己和竞品，
所以那个位置永远空着，而我们有 **924 条真实规格**。

现成题目（全部来自数据，不是想出来的）：

| 题 | 依据 |
|---|---|
| **圆筒锁功能五横评**（ET/BK/PS/CR/CL） | Clarity 上「我该选哪个」类查询已在发生 |
| door stopper **ds013 vs ds011** | Bing 上有人搜，我们没有页面 |
| 执手族横评 LH852/853/855/8826/8827 | 同族、规格齐全 |
| 闭门器横评（开启角、调速、门宽上限） | 规格可比 |
| euro cylinder lengths | 份额只有 18.92%，是强相关题里最弱的 |

### 10. 继续全栈筛查过期/错误数字 · **Claude**

今天一共查出 **12 个已发布或已记录的过期/错误数字**。全是同一个模式：
**某次手工数出来、写进文件、然后目录变了而数字没变。**

| 已修 | 原值 → 真值 |
|---|---|
| handing 覆盖 | 186 → 184 |
| 中心距 | 148/145 → **41**（103 条其实是拉手孔距） |
| 门厚覆盖 | 277 → 285 |
| Forend 覆盖 | 6 → 10 |
| Finish 覆盖 | 327 → 325，不同取值 85 → 84 |
| 黑色饰面行 | 30 → 36（八种拼法） |
| 尺寸图 / 门孔图 | 85/96 → **84/95** |
| DK / KT / IK 数量 | 10/16/2 → **9/12/3** |
| 「被引最多的文章」 | 型号那篇 → push-bar（44 次） |

`scripts/spec-coverage.mjs` 现在守住其中六个，并升级成「一个标签可映射多字段、
靠值区分」—— 因为我犯了两次同类错误：第一次正则太松，第二次**标签本身有歧义**。

**剩下的还没有生成器守着。** 这比再写十篇文章更值得做：一个买家抓到一个编造的
数字，会把这一页上所有数字都打折，包括对的那些。

### 11. JS 瘦身 · **Claude**

已测数据：首页 690 KB / 12 个 chunk，guides 文章页 679 KB —— **几乎一样**，
所以重量在共享外壳，不在页面内容。2.76 MB 那个 chunk 只在 `/model-lookup/`
（3 个页面）加载。两个 1.04 MB 的 chunk 被其他 chunk 动态引入，**不是死代码**
（已通过 `_buildManifest` 和交叉引用验证）。搜索索引 457 KB。

方案记在 `docs/collaboration/tasks/2026-09-21-two-week-seo-geo-plan.md` 第七节。

### 12. `__next._tree.txt` 被抓 3,000 次 · **Claude** · 低优先

`robots.txt` 已写 `Disallow: /*.txt$`，但这些 Next.js 内部文件仍被抓了约三千次。
**不致命，是纯浪费。**

修法两层，**先做第二层**：
1. 把 `llms.txt` 做厚（目前只是索引），让它真的包含结构化的产品与文章摘要 ——
   AI 爬虫抓 `.txt` 是因为想要纯文本，喂饱它比堵它有用
2. robots.txt 改成明确路径 `Disallow: /_next/`，`$` 锚点在某些实现里不生效

只堵不引，它们会去抓别的。

### 13. 增加语种：土耳其、法语、日语、韩语、德语、俄语 · **排在第 8 项之后**

**你 2026-09-22 明确要加。** 我原本建议不加，理由是 AI Overview 展示的前十个
国家里八个是英语优先市场，俄日韩阿在数据里一次都没出现。你重申了，按你的定。

**唯一的前置条件**：先做完第 8 项（西语统一到拉美）。
理由不是优先级，是**代价**：现在西语自己都不自洽（同一个产品页两个词指一个
东西），在这个基础上复制七份，等于把不一致复制七遍，将来要修七次。

先把一棵树修直，再种第二棵。

---

## 三、随时要记得的

- **每次发布后你 purge 一次。** 我只负责构建、提交、推送，然后写一句提醒。
- 服务器每五分钟自己拉一次代码，页面内容不需要你动手。
- 需要你动手的只有 nginx 跳转规则（在宝塔管的目录里）和各后台的配置。
- `deploy/install-nginx-redirects.sh` 要先 `cd` 进仓库再跑：
  `cd /www/wwwroot/cantonlock.com && sudo bash deploy/install-nginx-redirects.sh`

---

## 四、已完成（保留作记录，不要再做）

| 日期 | 事项 |
|---|---|
| 2026-09-22 | GA4 `generate_lead` 事件（代码）+ 你标记关键事件 |
| 2026-09-22 | www 与裸域合并，实测 301 生效 |
| 2026-09-22 | 第二批 20 篇 guides 部署，`/guides/` 共 40 篇 |
| 2026-09-22 | 落地 Codex 的 guides 分类库（40 个 slug 对账零误差） |
| 2026-09-22 | 旧文扩写批 1、批 2 共 6 篇 |
| 2026-09-22 | DK 订货代码由你书面确认 = 锁头双开 |
| 2026-09-22 | 15 款外装锁壳体材料按工厂表改正 |
| 2026-09-22 | 12 个过期/错误数字改正 + 生成器加固 |
| 2026-09-21 | `/guides/` 第一批 20 篇上线 |
| 2026-09-21 | 两个月 SEO/GEO 复盘报告（14 份报告，42 张表） |
| 2026-09-17 | `/index.asp` 跳转首页 |
