# Codex — approved black/gold inquiry confirmation

- User selected B's warm-white/black/gold design plus C's falling-paper motion. `InquirySuccess.tsx` and its CSS module replace the submitted form with an accessible confirmation, bilingual catalogue navigation (EN/ES/PT), another-inquiry action and animation-stop control. Gold/black side ribbons open first; a second layer falls from above. One 4.5-second canvas animation, lower mobile particle count, no sound, no pointer interception, stops when hidden/resized, respects reduced motion. No invented response-time promise or receipt number.
- `InquiryForm.tsx` mounts this state only after verified provider success, prevents concurrent sends, clears controlled product/model fields after success, and restores name-field focus for a new inquiry. Failures preserve inputs and existing email fallback. A 20-second deadline bounds both the HTTP request and JSON response; timeout copy explicitly says delivery is unconfirmed and may already have occurred.
- `src/lib/inquiry-submit.ts` and its three behavioral tests cover strict boolean success, provider HTTP/application failure, invalid JSON, request stalls and response-body stalls. Added to `npm test`.
- `npm run check` now runs the existing `assets:prune` after build, matching the existing deployment pipeline's stale-error-page cleanup. A fresh committed export repeatedly regenerated 24 known legacy soft-404 pages; pruning before build did not resolve it. This change uses the existing error-marker-based guard, not an invented deletion list.
- Browser checks use a local-only dummy key and intercepted Web3Forms responses: EN desktop success, ES/PT mobile success, application failure, real 20-second timeout, retained input, return focus and reduced motion. No email sent. A pre-existing React hydration error appears on contact page load before form interaction; the current GTM/head-hoisting change is outside this feature's scope. Do not claim a zero-error browser console.
- The isolated validation export under `tmp/codex-contact-release` uses a dummy key and MUST NOT be deployed. Production export must come from the main checkout with its real environment. Existing generated-data/image edits and preview refinements are left untouched.
- Validation: complete isolated `npm run check` passed (338 unit tests; 2,092 pages, 163,473 links, 50,701 asset references; no blocking semantic issues). Main checkout production build, stale-export prune, asset retention, `npm run test:export` and predeploy freshness all passed with the real environment. Existing 243 editorial length warnings are report-only. Main check initially ran the old command before the new prune step was saved; the corrected complete command passed in the isolated worktree.
- Source and the main checkout's complete static export are being committed together for release. Never deploy the isolated dummy-key export. Cloudflare purge remains client-only.
