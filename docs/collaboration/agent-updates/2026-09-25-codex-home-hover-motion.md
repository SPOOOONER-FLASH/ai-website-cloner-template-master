# Codex — HYDE home card motion, 2026-09-25

- Changed only `src/app/globals.css`: the home accent surface now holds its position on hover and keyboard focus; its existing frame, hard shadow, and title marker remain. This finishes the small card-feedback comparison from the 2026-09-22 motion audit without changing the approved visual language.
- The Impeccable detector returned no findings; the isolated checkout at `0791cfe8816` with this exact CSS passed `npm run check` (365 unit tests, 26 export tests, zero semantic SEO issues, dead links, or Chinese text on HYDE pages). A second isolated checkout at `045fee92b00` compiled and passed 366 unit tests, but its final export check stopped at an already-stale `docs/research/SPEC_COVERAGE.json`; four Claude-owned guides quote the changed counts. No shared `out/`, `out-rayen/`, guide copy, or Rayen source was touched.
- The earlier mobile carousel crop repair is already live: both 400w and 800w public URLs return 200 and match the committed `public/` and `out/` bytes by SHA-256. Live browser interaction recording timed out, so smoothness is supported by the previous local 390px rotation check, not a new production recording.
- Next: Claude should synchronize the coverage report and four guide figures before the next full HYDE release. The separate title-generator range bug affects 43 EN titles and DV05's disjoint ranges; `content/products/` is Claude-owned, so this note leaves those records untouched. Push this CSS commit with `npm run ship`; current pending guide-image commit has an unrelated conflict in `scripts/import-guide-heroes.mjs` and needs its owner to resolve it.
