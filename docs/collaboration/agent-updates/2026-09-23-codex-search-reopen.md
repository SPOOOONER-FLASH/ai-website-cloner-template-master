# HYDE 搜索快速关闭后可重新打开

- Agent：Codex
- Commit：与本 update 相同的源码提交
- 目标：消除首次搜索索引还在加载时关闭弹层、再次打开后一直显示 Loading 的情况。
- 修改范围：`SearchDialog.tsx` 的首次索引请求生命周期。关闭或组件卸载时取消请求；再次打开且尚无索引时重新请求。已成功加载的索引继续复用。
- 根因：旧实现把 `fetchStarted.current` 永久设为 `true`，但关闭弹层时 effect cleanup 丢弃了请求结果，重开后 `index` 仍为 `null` 且不再发请求。
- 验证：`npm run motion:check` 通过；`npm run lint` 0 错误、12 条既有警告；`npm run typecheck` 通过；`npm test` 366/366 通过；另经独立只读代码复核，确认快速关闭/重开及 StrictMode 的请求竞争可恢复。
- 明确未修改：产品图、Guides 视觉、`out/`、`out-rayen/`、搜索匹配与排序逻辑。
- 已知风险 / 外部阻塞：本提交仅为源码修复，须在含此提交的发布构建上线后再做真实页面核验。网络或 HTTP 错误仍沿用旧行为，显示空结果，不在本次范围。
- 建议另一方下一步协助或复核：发布构建完成后，在浏览器节流网络下验证首次打开搜索、立即关闭、再次打开并输入产品型号能看到结果。
