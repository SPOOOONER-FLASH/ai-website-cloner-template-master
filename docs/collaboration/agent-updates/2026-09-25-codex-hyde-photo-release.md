# Codex — HYDE photo release and public verification, 2026-09-25

- The client asked to publish the 13 real-photograph Blender presentation cards and retain current Guides imagery. The 13 official cards were already in the tracked `/product-studies/` gallery; EN, ES and PT pages reference all 13, and all 13 public 1440px WebPs matched the repository assets by SHA-256. No duplicate gallery or image rewrite was made. All 20 current public guide heads also matched the real catalogue-source WebPs by SHA-256, so the release leaves their displayed images unchanged.
- HYDE-only release commit `905823293bc` built source `0e8f7649e2f`, passed `npm run deploy:prep`, changed 13,725 files entirely under `out/`, and was verified on remote `main`. The same source and output subsequently passed full `npm run check`: 420 unit tests, 26 export tests, 2,298-page dead-link audit, zero blocking semantic SEO issues. Windows CRLF generator guards were normalized before the full check; there was no resulting source diff.
- Public checks after the push: `/`, `/product-studies/`, `/guides/`, and the new custom-tooling guide in EN/ES/PT all returned 200. The new guide's 15 referenced `_next/static` paths matched the exported HTML; six sampled CSS/JS assets returned 200. No Cloudflare purge was performed; the client handles that step.
- First release build had an unexplained Windows Next worker fail-fast while using 31 workers; one unchanged retry passed. A manually interrupted run left a temporary Git index lock, so the final release used a separate short-path worktree with four static workers. This is an operational workaround, not a demonstrated root-cause fix. Two earlier temporary worktrees were left untouched; no RAYEN source or output entered the release commit.
- Next: audit real product photographs for subject scale, baseline and background consistency before retouching; obtain factory evidence before presenting unconfirmed lock/cylinder/handle sets as compatible or modelling missing CAD features. The premium studio reshoot remains a photography dependency.
