# 2026-09-25 Codex — remove rejected composites from guide covers

- **Finding:** The 20 product images the client rejected were not merely dormant references. `scripts/import-guide-heroes.mjs` had copied all 20 into `public/images/editorial/guides/` and 20 published guide records reference those paths. The earlier 2026-09-24 note was correct only about the *library* path, not its imported copies.
- **Changed:** Replaced those 20 guide WebPs byte-for-byte with their documented original catalogue photos; updated all 20 provenance sidecars; blocked the old importer’s `--write` path so it cannot regenerate the false-background images. Added a reproducible, SHA-verified correction script and production brief under `docs/design-references/2026-09-24-guide-photo-correction/`. Guide URLs/copy and 13 separate architecture images were not touched. No `out/` or `out-rayen/` work was done.
- **Verification:** The correction script verified all 20 source hashes and output hashes, and its repeat `--write` made zero changes. `node --check` both scripts, targeted ESLint, `git diff --check`, and `npm test` (367/367) passed. The old importer correctly exits 1 before writing. A sample corrected guide image was visually inspected. Full `npm run check` was not run in this shared checkout because 17,544 `out/` and `out-rayen/` paths are dirty and another agent owns those outputs; use the isolated HYDE release checkout after source push.
- **Risk / next:** These truthful white-ground catalogue photos are a stopgap, not the premium dark-ground reshoot the client requested. The two actual in-camera dark photos under `public/images/editorial/` show the target grammar but do not identify the 20 guide SKUs. Claude owns article copy; keep its titles and model labels aligned with the originals and never re-run the old importer. Codex owns the image asset correction and HYDE release; RAYEN is untouched. FSB-like full product motion requires real macro footage or complete approved CAD, not a flat-photo zoom.
