# Codex — architecture + material diptych candidates

- Generated four new 16:9 architecture/material diptychs from the client's reference grammar:
  coastal limestone/stainless, concrete/graphite, oak/bronze, civic stone/aluminum.
- Enforced the current image rule: the scenes contain no identifiable door hardware and the
  material panels are abstract, unperforated surfaces rather than imagined products.
- Archived the supplied reference, four candidates and full prompt/usage boundaries under
  `docs/design-references/2026-09-09-editorial-diptychs/`.
- Verified all four generated files open as 1672×941 PNGs; visually checked the architecture
  and material panels for forbidden identifiable hardware, embedded copy and watermarks.
- No `src/`, `content/`, `public/`, `out/` or `out-rayen/` file changed; nothing was deployed.
  These remain selection candidates and must not be described as a HYDE project or finish sample.
  `npm run check` was not run because this checkpoint changes reference PNG/Markdown only and
  another active claim owns the generated exports; application source is untouched.
