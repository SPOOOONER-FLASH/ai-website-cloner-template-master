# 发布前规格覆盖数字同步

- Agent：Codex
- Commit：与本 update 相同的源码提交
- 目标：解除最新目录增长触发的 `specs:coverage:check` 发布阻断，并保持文章中公开的数字与目录一致。
- 修改范围：由 `scripts/spec-coverage.mjs` 重新生成 `docs/research/SPEC_COVERAGE.json`；四篇指南的英／西／葡文本把目录总数 964 更新为 973；硬件改造调查的门厚覆盖数由 318 更新为 325。
- 依据：生成器统计 973 个产品，门厚 325 个产品；图纸数仍为 84 张尺寸图、95 张开孔图，finish 等其余被引用的字段未变。
- 验证：`node scripts/spec-coverage.mjs --check` 通过；四个 JSON 均可解析；`content/` 和 `src/data/` 中已无旧的 964、指南中无旧的 318。
- 明确未修改：产品事实字段、产品图、搜索匹配、图纸几何及 `out/` 的发布版本。
- 已知风险 / 外部阻塞：当前主线在构建期间新增新闻源码和守卫，发布导出必须从新的主线重做，不能提交此前的旧构建。
- 建议另一方下一步协助或复核：内容同事可复核四篇文章的本地化数字；发布完成后以线上页面核验显示值。
