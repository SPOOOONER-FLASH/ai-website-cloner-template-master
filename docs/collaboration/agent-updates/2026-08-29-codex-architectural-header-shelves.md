# Codex · Architectural header shelves

| | |
|---|---|
| Scope | Desktop Company / Buy it now shelves; mobile route parity; Alibaba hard-shadow CTA |
| UX | Full-width overlay, no layout shift; click, hover, focus, Escape and outside-close |
| Tests | `npm test`; `npm run typecheck`; `npm run lint`; browser geometry at 1603px and 390px |
| Untouched | Product data, editorial images, Spanish product routes, existing `out/` until release build |
| Risk | Alibaba remains an external storefront; no onsite checkout was introduced |
| Next | Preview four pointer-follow magnifier variants before replacing the current large-image viewer |
