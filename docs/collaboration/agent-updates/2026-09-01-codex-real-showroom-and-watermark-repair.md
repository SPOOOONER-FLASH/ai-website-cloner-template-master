# Codex update — real showroom home image and product watermark repair

- **Agent:** Codex
- **Scope:** Replace the artificial nine-family home image with a real showroom derivative; rebuild and visually audit HYDE-branded product derivatives; confirm the 564 MB black hero delivered by the concurrent catalogue commit.
- **Source integrity:** `public/images/products/**` originals were not changed. The homepage derivative keeps the photographed showroom, hardware, perspective, lighting, and geometry from `public/images/company/showroom-emergency-hardware.webp`; only the obsolete illuminated oval sign was removed.
- **Homepage result:** `home.ts` and `home-es.ts` now use `hyde-nine-families-showroom-2026.webp` plus 480/960/1440 responsive variants. The previous AI sample-board image remains in the asset inventory but is no longer used on the homepage.
- **564 MB:** commit `44135b9abf` changed the product hero to the existing black source photograph `564-mb-night-latch-and-rim-lock-3.webp`; Codex did not duplicate that already-committed change.
- **Watermark result:** 1,485 formal derivatives checked: 933 adaptive HYDE marks, 409 edge-matched local repairs, 52 boundary-continuous fills, and 91 Big-LaMa inpaints. The old black oval, red script/registered-mark residue, tagline, pale blocks, partial semicircles, and product/banner collisions were reviewed through 38 full-set contact sheets plus 7 enlarged LaMa-only sheets. A red-residual scan produced four false positives caused by brass/wood pixels; all four were opened at full size and contained no old logo.
- **Regeneration:** run `python scripts/inpaint-legacy-product-images.py --input-root public/images/products --output-root tmp/codex-watermark-lama-all-v1`, then `node scripts/watermark-product-images.mjs --all --repair-root=tmp/codex-watermark-lama-all-v1 --require-repair-root --output-root=public/images/products-hyde --manifest=docs/design-references/product-watermark-manifest.json`. Big-LaMa checkpoint SHA-256: `7ba7aa7ac37a4d41fdbbeba3a2af7ead18058552997e3a3cd1a3b2210c9e6b4c`; Python pins are in `scripts/requirements-watermark-inpaint.txt`.
- **Checks completed before commit:** `npm run assets:watermark:check` (1,485/1,485); editorial srcset check (84/84); 17 focused Node tests; ESLint on changed JS/TS paths; `git diff --check`.
- **Untouched work:** No unfamiliar working-tree changes remained at checkpoint; other-agent source and release commits already present in HEAD were preserved.
- **Next:** run repository-wide non-build checks, commit and push these owned paths, then claim the clean `out/` baton, run `npm run deploy:prep`, push the generated release, and verify the no-query public homepage and representative product pages at the edge.
