# Codex — motion baseline exposed homepage hydration failure

- Read the 2026-09-22 design/motion audit and captured a **before** browser sequence against the current static export at 390 and 640px. Local artefacts: `tmp/codex-motion-baseline/home-390-before.mp4`, `home-640-before.mp4`, screenshots and `measurements.json`. Captures use actual Edge frames encoded at 8fps with the repository's existing ffmpeg; they show layout/copy changes, not a precise frame-rate profile.
- At 640px, slide 1 → 2 moved the caption bottom from 833.3 to 858.3px (25px). At 743px the hero frame was 700.3×525.2 while at 744px it was 701.3×275.4, confirming the 640px image / 744px frame breakpoint mismatch. Computed hero heading and body font sizes both resolved to 16.47px at 640px, confirming the two undefined classes inherited the same scale.
- The capture also logged React hydration error #418 on the homepage. Dev mode traced it to `FeatureColumns`: a whole-card Next Link wrapped an inner `ArrowLink`, producing nested anchors and browser DOM repair. `FeatureColumns.tsx` now keeps the whole-card link and renders the CTA as a styled noninteractive span. No destination or card content changed.
- Verification: after the fix, dev mode had no hydration error; the static export in EN/ES/PT returned 200, each card click navigated to its locale's article, and all three had zero page errors. `npm run check` passed (362 unit tests, 26 export tests); 2,218 pages and 175,904 internal links audited without broken targets. Post-build stale-page pruning, asset retention, export checks and predeploy check passed. `out/` and `out-rayen/` were rebuilt from this source.
- Next visual work: align hero frame/image/preload at 640px, reserve the caption height, then tune search and image zoom motion against the before capture. Preserve real product pixels and reduced-motion behavior. Avoid interpreting this 8fps clip as evidence of frame timing.
