# Codex — mobile carousel crop hotfix

- Scope: HYDE homepage mobile carousel. The second slide referenced `hero1` instead of an inline media object; `build-mobile-hero-crops.mjs` matched only inline `src` text in `heroCarousel`, so it missed both warm-entry crops and its `--check` falsely passed. The live 390px browser requested the missing 400w crop (404, `naturalWidth=0`) and stayed on slide one.
- Changed: `scripts/build-mobile-hero-crops.mjs` now reads the three locale carousel declarations with the TypeScript AST, resolves referenced slide objects, and checks all real slide sources. Added the two missing 4:3 crops from the existing editorial source (400w 12,460 bytes; 800w 41,508 bytes). Corrected the fallback comment in `HeroCarousel.tsx`. `scripts/build-product-titles.mjs --check` now skips Rayen-only records, matching the HYDE route predicate in `src/data/products.ts`; no Rayen product JSON was changed.
- Verification: before generation the crop guard reported exactly two missing files, afterwards `all present`. At 390px on a local static export the second image returned 200 and became active around 7 seconds, then the third became active; no 404 or console errors. In an isolated clean checkout at `0791cfe8816` with these exact source/assets, `npm run check` passed: 365 unit tests, 26 export tests, 2,220-page dead-link audit, no semantic SEO errors.
- Untouched: Rayen output/source and other agents' article/product changes. The shared checkout's full check was blocked by `SPEC_COVERAGE.json` aging as Rayen-only products were concurrently committed; the isolated check above establishes the hotfix's green result without those moving changes.
- Next: publish the HYDE export through `npm run release:hyde` after current committed-source coverage figures are synchronized; verify both public crop URLs and the 390px slide transition. Separately, 39 HYDE product titles collapse a published door-thickness range to its upper bound (e.g. `35–55mm` → `for 55mm doors`); correct the generator's range parsing and regenerate those titles in a focused follow-up.
