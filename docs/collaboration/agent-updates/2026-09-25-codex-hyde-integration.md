# Codex — HYDE guide imagery and motion integration, 2026-09-25

- Merged the local 20-guide image correction and home accent hover refinement onto the latest `origin/main` in an isolated worktree. Kept the remote American-spelling edit and the importer's immediate `--write` rejection guard. Resolved the generated ship log from current history and preserved both sides of the push-pending record. No RAYEN source or release output is in the integration commit.
- Fixed three JSX apostrophes in the EN certifications/contact pages that blocked current-main lint; copy and claims are unchanged. The merged source passed `npm run check`: 420 unit tests, 26 export tests, 2,295-page dead-link audit, zero semantic SEO blockers, and no Chinese text on HYDE pages. The correction script dry-run verified 20 true catalogue-source guide photos, the old importer rejected `--write` before changing a sampled asset, and the five guide-image uniqueness tests passed.
- Claude's HYDE-only specification coverage report and four guide figures were already present on `origin/main`; `specs:coverage:check` passed, so no duplicate article edits were made. The isolated check rebuilt both outputs because Next.js shares a build, but only a later `release:hyde` may commit `out/`; do not stage `out-rayen/`.
- Next: push the merged source via `npm run ship`, run `npm run release:hyde` from a clean checkout, verify all 20 exported guide images byte-match the corrected `public/` sources and public guide pages, then ask the client for their Cloudflare purge. The dark-ground studio reshoot remains a separate photography dependency; no invented product scene or compatibility claim is implied by these truthful catalogue-photo stopgaps.
