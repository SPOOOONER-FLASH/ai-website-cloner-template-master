# 9004S published exterior study

This is one incomplete exterior model, not a manufacturing model or approved product image.
The model, orthographic view and dimensions are generated by `scripts/blender/9004s-published-shell.py`.

```powershell
& 'C:/Program Files/Blender Foundation/Blender 5.2/blender.exe' --background --python-exit-code 1 --python scripts/blender/9004s-published-shell.py -- docs/design-references/2026-09-07-home-stills/models-9004s
```

Success requires `BLENDER_OK`, a nonempty `.blend` and preview, and dimensions within 0.0001 mm of the expected exterior solids. The script verifies closed mesh edges. The catalogue dimension image is packed inside the Blender file; its SHA-256 is recorded in `9004s-evidence.json`.

Published geometry: 135 mm lever length, 20 mm height, 10 mm front bar depth, 20 mm neck width, 63 mm overall projection, 53 × 53 × 8 mm rose. Undimensioned edge radii, inset trim, set screw, mounting holes, interior mechanism, spindle length and escutcheon are omitted. The plain outer shape must not be presented as the complete 9004S product.

## Remaining model inputs

- LC04: need lock faceplate length/width and its datum to the 173 mm case; four bolt centres/spacing; latch geometry; case thickness; all featured bore and fixing positions. The 300 × 25 mm diagram dimensions refer to the separate strike and cannot automatically be assigned to the lock faceplate.
- BL028: the published 4 in × 3 in × 2.5 mm size does not establish hole centres/diameters, barrel diameter, bearing sections or leaf offsets. A dimensioned factory drawing is needed for those features.
- Panic devices: the available exterior pull-trim dimensions do not establish grip diameter/bend radii and cutout datums. The red crossbar and silver push-pad photographs establish appearance, not complete construction geometry.

No hole positions, curved handle paths or installation relations are to be inferred by measuring perspective photographs. Two more complete product models remain unfinished.
